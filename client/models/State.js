/**
 * require(vendor/backbone)
 */
(function(){

  window.State = Backbone.Model.extend({

    fields:[
      'locations',
      'location',
      'tower',
      'freqs',
      'editModel'
    ]

  })


}());
